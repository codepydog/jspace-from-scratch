[project]
name = "jspace-repro"
version = "0.1.0"
description = "A compact Jacobian lens demo for Qwen3.6-27B on Apple Silicon"
requires-python = ">=3.12"
dependencies = [
    "mlx",
    "mlx-vlm>=0.6.5",
    "numpy",
    "ipykernel",
    "ipywidgets",
    "matplotlib",
]

[tool.uv]
package = false
